const size = {
    desktop: '1200px',
    tablet: '768px',
    phone: '600px',
};

export const media = {
    desktop: `(min-width: ${size.desktop})`,
    tablet: `(min-width: ${size.tablet})`,
    phone: `(min-width: ${size.phone})`,
}